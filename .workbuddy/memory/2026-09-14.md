# 2026-09-14

## ConfigForm 任务：门禁回归修复 + 设计器物料面板 UI 对齐

背景：HEAD `e7892ff6`（“优化页面编译协调器与运行时事件协议”）提交时质量门禁为红，工作台测试 3 文件 / 36 项失败。

### 修复内容

- `workbench/src/app/__tests__/architecture-boundary.test.ts`：`misplacedContracts` 从纯文本正则改为 `@babel/parser` 顶层声明判定，消除「生成代码模板字符串被误判为类型声明」的假阳性。
- 类型归位：`runtime/src/renderer/types/runtime-actions.ts`、`runtime/src/renderer/types/array-rendering.ts`（新增）；测试夹子类型移入 `__tests__/types/`。
- `workbench/src/project/__tests__/business-scenarios-fixture.ts`：required 字段不再声明 `defaultValue: ''`；类型移入 `project/__tests__/types/business-scenarios.ts`。
- `workbench/src/project/__tests__/generated-runtime-module.ts`：新增 `@moluoxixi/config-form-vue-backend` 作为宿主外部依赖。
- `workbench/src/project/__tests__/business-scenarios-parity.test.ts`：`pageCompilation.snapshot` → `snapshotIdentity`（精确断言 committed 快照身份）。
- 两个组件库的 `detail-table` / `array-subform` / `object-group` 物料：`source.tag` 由物料名改为 `section`，与运行时 `Section` 组件渲染的 `<section>` 一致。
- `runtime/src/renderer/composables/use-renderer-events.ts`：`reportDiagnostic` 不再把 `severity: 'warning'` 的创作期诊断经 `flowError` 抛出（warning 仍在 dispatch 结果与 trace 中）。
- UI：设计器物料面板单列列表 → 两列图标磁贴（`designer/src/styles/designer-palette.scss` + `workbench/.../StudioLeftPanel/style/index.scss`），对齐 FormCreate Pro / VForm 3 Pro；`theme-contract.test.ts` 调色板断言同步更新。

### 验证结果

- 工作台：36 项失败 → 1 项失败（76/77 通过的 parity 集 + 全量 67/69 文件）。
- 架构门禁 19/19 通过；`theme-contract` 与 `business-scenarios-parity` 全绿。

### 未解决（非本轮引入）

- 工作台 `config-runtime-parity`：自定义校验器被正确调用并返回消息，但 issue 未进入控制器错误表。
- 「行身份 / itemKey」缺陷簇：headless `controller-model-identity` 与 `controller-scope-refresh` 共 4 项；runtime `nested-materials` 2 项（复制行连带复制业务主键）。建议同轮修复。

### 环境注意

- 本机 `Bash` 工具不可用（shim 报错、退出码 127）；`PowerShell` stdout 不回传，需「写文件 + 读取」。
- vitest 会调用 `wmic.exe`，被安全策略拦截并偶尔中断命令输出，但测试实际已执行完毕，产物可读。
- 后台长命令结束时用 `Grep` 检索日志，避免用 `Read` 直读大文件。
