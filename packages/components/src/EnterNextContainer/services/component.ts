import { withInstall } from '../../utils'
import { EnterNextContainerSource } from '../components'

export const EnterNextContainer = withInstall(EnterNextContainerSource)

export default EnterNextContainer
