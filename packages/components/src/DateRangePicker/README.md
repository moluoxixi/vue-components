# DateRangePicker

基于 Element Plus `ElDatePicker` 的日期/日期范围选择器。

```ts
import { DateRangePicker } from '@moluoxixi/components'
```

组件保留旧项目的主要 props：`type`、`format`、`valueFormat`、`outputFormat`、`defaultToday`、`dateRange`、`minDate`、`maxDate`、`disabledDateRange` 和 `shortcuts`。
