{
  "onSessionStart": [
    {
      "description": "Inject Moluoxixi session context on Snow session start (additionalContext + .snow/log breadcrumb)",
      "hooks": [
        {
          "type": "command",
          "command": "python -X utf8 .snow/hooks/write-moluoxixi-context.py session",
          "timeout": 20000,
          "enabled": true
        }
      ]
    }
  ]
}
