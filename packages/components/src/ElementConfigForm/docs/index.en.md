# ElementConfigForm

A schema-driven form for Element Plus field bindings. It uses the shared headless controller and native form renderer to support field configuration, validation, conditional visibility, read-only rendering, and form state management.

Define fields with `fields` and manage form values with `v-model`. The component follows Element Plus's `modelValue` field-binding convention and uses native CSS Grid and Flexbox for layout.
