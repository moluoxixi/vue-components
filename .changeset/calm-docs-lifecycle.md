---
"@moluoxixi/vitepress-theme-element-plus": minor
---

Add reusable repository providers, package-level component profiles, browser and Node repository entries, project-owned Markdown source links and external Playground projection, and the `element-plus-docs` prepare/dev/build CLI so a component library can configure documentation infrastructure without copying provider, metadata, or Markdown resolver scripts.
