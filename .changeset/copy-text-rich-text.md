---
"@moluoxixi/components": minor
---

Add `CopyText`, headless `HeadlessCopyText`, the `copyText` clipboard utility, and the Tiptap-based `RichTextEditor` with HTML v-model, formatting controls, custom toolbar support, and accessible disabled and readonly states.
